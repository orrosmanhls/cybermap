import React from "react";

import { Container } from "./styles";

interface Props {
  text: string;
}

const {{ pascalCase name }}: React.FC<Props> = ({ text }) => {
  return <Container data-testid="{{ kebabCase name }}">{text}</Container>;
};

export default {{ pascalCase name }};
